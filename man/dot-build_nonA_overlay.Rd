% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ninetails_plotting_functions.R
\name{.build_nonA_overlay}
\alias{.build_nonA_overlay}
\title{Build ggplot2 annotation layers for non-A residue highlights}
\usage{
.build_nonA_overlay(
  read_nonA_data,
  poly_tail_start,
  poly_tail_end,
  nonA_flank = 250,
  x_scale_factor = 1,
  alpha = 0.15
)
}
\arguments{
\item{read_nonA_data}{Data frame. Rows from the \code{nonadenosine_residues}
table for a single read. Required columns: \code{prediction} (C/G/U),
\code{est_nonA_pos}, \code{polya_length}.}

\item{poly_tail_start}{Numeric. Poly(A) start coordinate in raw signal.}

\item{poly_tail_end}{Numeric. Poly(A) end coordinate in raw signal.}

\item{nonA_flank}{Numeric. Number of raw signal positions to highlight
on each side of the estimated modification center. Default 250 (matching
the ~500-point raw signal extent of a 100-point interpolated chunk).}

\item{x_scale_factor}{Numeric. Scaling factor for x-axis coordinates.
Use 1 for raw position plots, \code{1 / sampling_rate} for time-rescaled
plots. Default 1.}

\item{alpha}{Numeric. Transparency of the highlight rectangles [0, 1].
Default 0.15.}
}
\value{
A list of ggplot2 annotation layers (can be added to a ggplot
  with \code{+}). Returns an empty list if \code{read_nonA_data} has
  zero rows or is NULL.
}
\description{
Creates a list of \code{ggplot2::annotate()} layers (semi-transparent
rectangles + residue labels) for each non-A modification in a single
read. The rectangles are drawn behind the signal line to highlight the
estimated modification position with a flank.
}
\details{
Residue colors match the package defaults used in
\code{\link{plot_residue_counts}} and \code{\link{plot_panel_characteristics}}:
C = \code{"#3a424f"}, G = \code{"#50a675"}, U = \code{"#b0bdd4"}.
}
\keyword{internal}
